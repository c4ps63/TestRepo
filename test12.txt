function jitter(values) {
  return values.map(v => v + Math.floor(Math.random() * 3) - 1);
}

const data = [10, 20, 30, 40];
const noisy = jitter(data);

console.log("Original:", data);
console.log("Noisy:", noisy);
